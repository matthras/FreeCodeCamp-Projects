$(document).ready( function() {

	// Display Array
	var dArray = [];

	// onClick function for each button
	var dUpdate = function(val) {
		dArray.push(val);
	}

	// Evaluate function for equals button
	var dEvaluate = function() {
		for(i=0; i<dArray.length; i++) {
			// Iterate through each element of array, performing function depending on input
		}

	}
	

});